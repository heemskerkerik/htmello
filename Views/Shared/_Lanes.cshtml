@using htmello.Lane
@model htmello.Data.BoardDto

@{
    var addLaneModel = new AddLaneModel(Model.BoardId, "");
}

<div class="lanes" 
     id="lanes">
    @foreach (var lane in Model.Lanes)
    {
        <partial model="@lane" name="_Lane"/>
    }
    <div class="add-lane">
        <partial name="_AddLane" model="addLaneModel"/>
    </div>
</div>